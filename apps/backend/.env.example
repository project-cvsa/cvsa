DATABASE_URL=postgres://cvsa:password@localhost:5432/cvsa
MEILI_MASTER_KEY=0712041211241099
MEILI_API_URL=http://127.0.0.1:7700
COOKIE_DOMAIN=projectcvsa.com
WEB_ORIGINS=http://zh.dev.projectcvsa.com:10199,http://en.dev.projectcvsa.com:10199,http://localhost:10199
# Required in production (better-auth refuses to boot without a secret).
# Must be identical to apps/web's value so both processes sign/validate the
# same session cookie; generate one e.g. with `openssl rand -base64 32`.
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=
