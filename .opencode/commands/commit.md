---
description: Write a commit message
---

Use VCS tools to check what has changed in the current commit, then write a commit message.

The commit message should follow the 50/70 rule and describe both the motivation for the commit and the changes it involves.

After drafting your first version, present it to the user and modify it if they give you any feedback.
