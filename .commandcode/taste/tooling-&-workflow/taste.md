# Tooling & Workflow
- Prefers a holistic review of related code before making a narrow fix, including component design, code style, external-library usage, and whether refactoring is warranted. Confidence: 0.9
- When a required tool appears unavailable, expects the agent to investigate PATH, installation locations, shims, and workspace dependencies and retry before concluding that the environment is missing it. Confidence: 0.95
- For UI fixes, expects screenshot/design evidence and, when appropriate and authorized, verification of the actual running page and user interactions (for example, rendering, focus, and button behavior) rather than treating successful compilation, tests, or builds alone as proof that the issue is fixed. Do not start a server arbitrarily or without permission. Confidence: 0.98
- Uses Jujutsu (`jj`) alongside Git for version control. Confidence: 0.8
- Squash-and-merge workflow: commit messages for squash merges should be comprehensive and descriptive like a full PR description. Confidence: 0.85
- When providing commit messages or suggested commands upon request, output them directly for user review rather than automatically applying them without instruction. Confidence: 0.8
- For frontend class management, prefers an appropriate established library for composing conditional classes rather than manual template-string or string concatenation. Confidence: 0.95
- Never modify `.env` files or environment configuration — user explicitly said "不要乱动env" after the agent edited `apps/web/.env` (the edit was rejected and had to be reverted). When an env value is needed for verification or running, inject it via the process environment at runtime (non-persistent) and provide the user a list of variables to add themselves. Confidence: 0.95
- User keeps their own dev servers running long-term with hot-reload (backend `--watch` on dev.projectcvsa.com:16412, Astro dev on zh.dev.projectcvsa.com:10199) — verify changes against these already-running servers (e.g., curl E2E with cookie jars) instead of starting or restarting them; never kill or reconfigure the user's processes. Confidence: 0.75
- Local dev environment: `*.projectcvsa.com` hostnames resolve to the local machine via the user's fake-ip DNS (e.g., api.dev.projectcvsa.com → 198.18.0.151 loopback), so CORS and cross-subdomain cookies work in dev without editing /etc/hosts or .env; if a dev server binds IPv6-only, use `curl --resolve host:port:[::1]` to reach it. Confidence: 0.7
- Standard verification pipeline for this repo: `bun x tsc --noEmit` per package, `bun x biome check` (with `--write` to autofix), `bun test` for the backend (reset the test DB first via `bun x prisma db push --force-reset` in packages/db with NODE_ENV=test), and `bun x vitest run` for web. Confidence: 0.7
- Documentation-first for framework/library integration: consult official docs before writing integration code (e.g., better-auth Astro guide, Astro session-management and middleware docs), and when docs are missing or inconclusive, verify against the installed package's source in node_modules rather than relying on memory or guesses — user explicitly instructed "多查文档，不要瞎写" (check the docs, don't write blindly). Confidence: 0.9
